# FIXES — proposed diffs (report-only; nothing applied)

Ordered by severity. Each section is independently applicable. Finding IDs refer to
`GO-LIVE-TEST-REPORT.md`. Constraints honoured: no legal-page edits, no CAI Score copy changes,
no translations added, env vars referenced by name only.

---

## §1 · CRITICAL — Revive checkout (F-01)

`STORE` in `public/agents.html` (~line 2606) needs three business values. **Decisions needed from you** (the harness can't invent prices): the price list and a Stripe **test-mode** Payment Link per agent (or one global link). Then:

```diff
   var STORE={
     currency:'EUR',
-    price:null,                     // e.g. 1200  â€” leave null to show "Pricing on request"
+    price:<PRICE_EUR>,              // decision needed — null keeps "request access" mode
     perAgentPrice:{},
-    checkoutBase:'https://www.colleagueai.ai/checkout', // your Stripe Payment Link / checkout endpoint
-    paymentLinks:{},
+    checkoutBase:'<STRIPE_PAYMENT_LINK_URL>',   // test-mode link until go-live sign-off
+    paymentLinks:{ /* per-slug overrides, e.g. "contract-summarisation-agent":"https://buy.stripe.com/test_..." */ },
     gate:{ copilotStudio:true, m365:true, dossier:false  },
-    schedulerUrl:'YOUR_SCHEDULER_URL'
+    schedulerUrl:'<SCHEDULER_URL or https://www.colleagueai.ai/demo>'
   };
```

Also make the static hrefs crawlable so the CTA is never `#` even before JS (`agents.html:1852,1871,2423`):

```diff
-<a href="#" id="nav-call-link" class="nav-cta" data-i18n="nav_demo">Book a call</a>
+<a href="https://www.colleagueai.ai/demo" id="nav-call-link" class="nav-cta" data-i18n="nav_demo">Book a call</a>
```
(same for `hero-call-link`; for `pay-cta`, point the no-JS default at `/demo` or the payment link.)
Regression cover: `tests/dom-smoke.test.mjs` #2/#6, `tests/e2e/golive.spec.js` tests 3/5.

---

## §2 · CRITICAL — Un-crash the i18n engine (F-02, F-03)

**This repair was validated in the harness** (scratch copy + jsdom: dropdown=8 options, `?lang=cs`
renders "Filozofie", `applyLang('de')` renders "Philosophie"). The engine is missing more than two
constants — four coordinated edits in `public/agents.html`, all inside the language script (script #5)
except (d):

**(a)** `ORDER`/`NAMES` (used to build the dropdown) are never defined. Insert after `window.applyLang=applyLang;`:
```js
var ORDER=["en","cs","de","fr","es","it","pt","pl"];
var NAMES={en:"English",cs:"Čeština",de:"Deutsch",fr:"Français",es:"Español",it:"Italiano",pt:"Português",pl:"Polski"};
```

**(b)** `I18N` is declared *inside* `applyLang`, but the init code reads `I18N[init]` outside it.
Hoist the dictionary out of the function (delete `var applyLang=function(l){` before `var I18N={`,
re-open the function after the dictionary — see (c)).

**(c)** `window.T` — the translation lookup `applyLang` depends on — **does not exist anywhere in the
file** (10 call sites, zero definitions; every locale lookup throws). Define it where the dictionary
now sits, and re-open `applyLang`:
```diff
-var applyLang=function(l){
-var I18N={ … all 8 dictionaries … };
+var I18N={ … all 8 dictionaries … };
+var CUR="en";
+window.T=function(k){var d=I18N[CUR]||I18N.en;return d&&d[k]!=null?d[k]:(I18N.en&&I18N.en[k]!=null?I18N.en[k]:null);};
+var applyLang=function(l){CUR=l;
 document.documentElement.lang=l;
```

**(d)** Stray `}` terminates the `AGENTS_CS` script block (script #12, ends `…"]}\n}\n}`):
```diff
 "pains": ["Reporting pro vedenÃ­ je ruÄnÄ› â€¦"]}
 }
-}
```

Apply §6 (encoding) together with this — the dictionaries themselves contain the mojibake.
Regression cover: `tests/dom-smoke.test.mjs` #7/#8 must go green; E2E tests 1 & 7.

---

## §3 · CRITICAL — Prerender every route; create the missing pages (F-04, F-07, F-14, F-16, F-19, F-20)

The repo's own pattern (static HTML in `public/` + `vercel.json` rewrite) already works — `/agents` proves it. Apply it to the rest:

1. **Create** `public/trust.html`, `public/terms.html`, `public/license.html`, `public/privacy.html`, `public/partner-agreement.html`, `public/demo.html` (the rewrites for them already exist in `vercel.json`). Legal pages: English-only drafts from counsel — **not generated by this harness by design**. `demo.html` can prerender the Demo UI's static frame (H1, copy, FAQ) and hydrate the interactive part.
2. Each new page head: self-canonical `https://www.colleagueai.ai/<route>`, descriptive `<title>` ("Trust Center — AI Governance Evidence | Colleague AI" pattern), Organization JSON-LD, robots `index,follow`.
3. **404:** add `public/404.html` + replace the catch-all rewrite so unknown paths stop returning the marketplace with 200:
```diff
-    { "source": "/(.*)", "destination": "/index.html" }
+    { "source": "/demo", "destination": "/demo.html" },
+    { "source": "/(.*)", "destination": "/404.html" }
```
   (Vercel serves `404.html` with status 404 when used as the not-found page; verify with `tests/audit-headers.mjs` P1 + E2E test 8.)
4. **Sitemap:** drop nothing — instead make the listed pages real (step 1), then add `/license` and `/partner-agreement`.
5. **Deploy** repo HEAD (F-23) — production currently runs an older config where even `/partners` is a shell.

Effort: 1–2 days incl. copy for trust/demo + counsel handoff for legal. This single section flips most Critical SEO findings.

---

## §4 · CRITICAL — Webhook: idempotency, failure handling, gating, sanitization (F-05, F-06, F-10, F-11, F-21)

`api/webhook.js` + `lib/db.js`. One coherent diff, four defects:

```diff
--- a/lib/db.js
+++ b/lib/db.js
@@
+// ─── Webhook idempotency ───────────────────────────────────────────────
+/** Returns true the FIRST time an event id is seen (atomic SET NX), false on replays. */
+export async function claimEvent(eventId, ttlSeconds = 60 * 60 * 24 * 30) {
+  const res = await kv('SET', `stripe:event:${eventId}`, '1', 'NX', 'EX', String(ttlSeconds));
+  return res === 'OK';
+}
+/** Same trick per checkout session so two different events can't double-credit one sale. */
+export async function claimSession(sessionId) {
+  const res = await kv('SET', `stripe:session:${sessionId}`, '1', 'NX');
+  return res === 'OK';
+}
```
*(Note: `kv()` builds path-style REST calls; `SET key value NX EX ttl` maps to `/SET/key/1/NX/EX/ttl` — supported by Upstash REST.)*

```diff
--- a/api/webhook.js
+++ b/api/webhook.js
-import { grantEntitlement, recordCommission } from '../lib/db.js';
+import { grantEntitlement, recordCommission, claimEvent, claimSession, getPartnerStats } from '../lib/db.js';
@@
   if (event.type === 'checkout.session.completed') {
     const session = event.data.object;
+    // 1. Idempotency: drop replays of the same event, and any second event for the same session.
+    try {
+      if (!await claimEvent(event.id))   return res.status(200).json({ received: true, duplicate: 'event' });
+      if (!await claimSession(session.id)) return res.status(200).json({ received: true, duplicate: 'session' });
+    } catch (err) {
+      console.error('[webhook] Idempotency check failed:', err);
+      return res.status(500).json({ error: 'KV unavailable' }); // Stripe retries
+    }
@@
     } catch (err) {
       console.error('[webhook] Failed to write entitlement:', err);
-      // Still return 200 — Stripe will retry on non-2xx. Log and investigate.
-      return res.status(200).json({ received: true, warning: 'Entitlement write failed — check logs' });
+      // Non-2xx makes Stripe retry with backoff — that is what we want.
+      return res.status(500).json({ error: 'Entitlement write failed' });
     }
 
-    const partnerCode = session.metadata?.partner;
+    // 2. Sanitize the ref: same contract as the client (≤64 chars, safe charset).
+    const rawCode = session.metadata?.partner ?? '';
+    const partnerCode = /^[A-Za-z0-9_-]{1,64}$/.test(rawCode) ? rawCode : null;
+    if (rawCode && !partnerCode) console.warn('[webhook] Dropped malformed partner ref on', session.id);
     if (partnerCode) {
+      // 3. Accreditation gate: only registered partners earn. Unknown codes are logged, not credited.
+      const partner = await getPartnerStats(partnerCode).catch(() => null);
+      if (!partner) {
+        console.warn('[webhook] Unregistered partner code, commission withheld:', partnerCode, session.id);
+      } else {
       const amountGross = session.amount_total ?? 0;
       try {
         const commission = await recordCommission({ code: partnerCode, amountGross,
           commissionRate: COMMISSION_RATE, stripeSessionId: session.id });
         console.log('[webhook] Partner commission recorded:', commission);
       } catch (err) {
-        // Non-fatal — entitlement is already granted; log and continue.
-        console.error('[webhook] Failed to record partner commission:', err);
+        console.error('[webhook] Failed to record partner commission:', err);
+        return res.status(500).json({ error: 'Commission write failed' }); // retry; claimEvent above is idempotent
       }
+      }
     }
   }
```

Also remove the stub-creation branch in `lib/db.js#recordCommission` ("Unknown code — create a stub") — the gate above makes it dead code, and it is the mechanism behind F-10.

**Recurring revenue (F-11) — decision needed, then code.** Current rule: only `checkout.session.completed` credits; renewals pay 0. If subscriptions will be sold, state the rule (e.g. "20% of first 12 invoices") and add an `invoice.paid` branch keyed by `subscription.metadata.partner`, idempotent on `invoice.id`. If only one-time sales: document it and the harness keeps asserting renewals stay uncredited.

Verification: `node --test tests/webhook.test.mjs` → all 14 must pass (3a/3b/5c/7/8 currently document the defects).

---

## §5 · HIGH — One canonical host, self-referencing canonicals (F-08)

1. `index.html` (SPA shell) line 8:
```diff
-    <link rel="canonical" href="https://colleagueai.ai/" />
+    <link rel="canonical" href="https://www.colleagueai.ai/" />
```
   This is a stopgap; the real fix is §3 (per-route prerendered heads). The same file's `og:url` should match.
2. Locale URLs must self-canonicalize once they serve localized content (§7). Until then they are EN duplicates and `/agents` as canonical is the lesser evil — flagged, not diffed.
3. Add the redirect `colleagueai.ai → www` at the Vercel domain level (it already 30xes; confirm it is a single hop to the same path, not to `/agents`).

---

## §6 · HIGH — Fix the encoding corruption (F-09)

The file is double-encoded (UTF-8 read as cp1252, re-saved as UTF-8) with BOM. Mechanical, lossless repair:

```bash
python3 - <<'PY'
import codecs
raw = open('public/agents.html', encoding='utf-8-sig').read()
fixed = raw.encode('cp1252', errors='backslashreplace').decode('utf-8', errors='replace')
assert 'â€”' not in fixed and 'Ã©' not in fixed
open('public/agents.html.fixed', 'w', encoding='utf-8').write(fixed)
PY
# review the diff, then replace the file; keep editors/pipelines pinned to UTF-8 (add .editorconfig)
```
Verify: `node tests/audit-i18n.mjs` mojibake counts must read 0 for all 8 locales; headers-audit M1 green. Check `public/partners.html` and the React sources too (currently clean).

---

## §7 · HIGH — i18n architecture migration (F-12, F-22) — proposal + effort, NOT implemented

Target: locale-prefixed, prerendered routes with hreflang clusters.

1. Build step (Node script in `scripts/`) renders `agents.html` × 8: applies each dictionary server-side (the `applyLang` logic is deterministic DOM substitution — reusable in jsdom), writes `dist/agents.html`, `dist/cs/agenti.html`, … Each output: only ITS locale inline (≈70 KB less per page; EN page drops ~7 dictionaries → est. 300–400 KB raw saving), `<html lang>`, localized title/meta, self-canonical, full hreflang cluster (already correct in the head), `og:locale` per page + all 7 `og:locale:alternate`.
2. `vercel.json`: point locale rewrites at the new per-locale files (drop `?lang=`).
3. Keep the client switcher as navigation between locale URLs (no re-render).
4. Sitemap already carries the 8 alternates — becomes truthful the moment the URLs serve localized bytes.

Effort estimate: build script 1–2 days, QA across 8 locales 1 day, rollout behind a preview deploy 0.5 day. Prerequisites: §2 (engine must work) and §6 (fix encoding first or you bake mojibake into 8 files).

---

## §8 · HIGH — Structured data (F-13)

1. Extract the existing `@graph` (Organization, FAQPage, …) from `agents.html` into a shared snippet and emit Organization JSON-LD on every prerendered page (§3).
2. Add Product JSON-LD per agent on `/agents` (and future per-agent pages): name, description, brand=Colleague AI, offers (price once §1 lands — `priceCurrency:"EUR"`, `availability:"https://schema.org/InStock"`). With 36 products on one page, emit an `ItemList` of `Product` nodes.
3. Re-validate with `node tests/audit-headers.mjs` (S1/S3 parse-based) + Google Rich Results test.

---

## §9 · HIGH — Stop dropping attribution on Book-a-call (F-15)

The scheduler-fallback script (agents.html ~line 2802) overwrites the partner-tagged hrefs. Make it re-apply the ref:

```diff
-<script>(function(){var u=(window.STORE&&STORE.schedulerUrl&&STORE.schedulerUrl!=="YOUR_SCHEDULER_URL")?STORE.schedulerUrl:"https://www.colleagueai.ai/demo";["nav-call-link","hero-call-link"].forEach(function(id){var el=document.getElementById(id);if(el)el.href=u;});})();</script>
+<script>(function(){var u=(window.STORE&&STORE.schedulerUrl&&STORE.schedulerUrl!=="YOUR_SCHEDULER_URL")?STORE.schedulerUrl:"https://www.colleagueai.ai/demo";["nav-call-link","hero-call-link"].forEach(function(id){var el=document.getElementById(id);if(!el)return;try{var url=new URL(u,location.origin);if(window.__cai_partner)url.searchParams.set("partner",window.__cai_partner);el.href=url.toString();}catch(e){el.href=u;}});})();</script>
```
Verify: `tests/dom-smoke.test.mjs` #4 goes green; E2E test 6.

---

## §10 · HIGH (counsel) — Translated legal wording in `foot_legal` (F-17)

All 7 non-EN dictionaries carry a translated ~610-char licence notice. Per policy (legal = English-only drafts for counsel), the conservative fix is to replace the translated values with the English notice + a localized *non-legal* pointer sentence ("Legal terms are provided in English") — **wording for counsel to approve; no diff applied, and this harness has not modified any legal text.**

---

## §11 · MEDIUM — CSP hardening (F-18)

`script-src 'unsafe-inline'` is structural (the site is built on inline scripts). Realistic sequence: (1) §3/§7 move pages to build-time generation → (2) add hashes (`'sha256-…'`) for the remaining inline blocks at build time → (3) drop `'unsafe-inline'` from `script-src` in `vercel.json`. Defer until the prerender work lands; flagging now per grader impact (A-, not A+).

---

## Order of application

§2 (2 lines, unlocks 8 locales) → §4 (revenue integrity) → §1 (needs your price/link decisions) → §6 (before §7 bakes files) → §3 → §5 → §9 → §8 → §7 → §10/§11.
